workflows:
  android-workflow:
    name: Ai Studio Pro Android
    instance_type: mac_mini_m1
    max_build_duration: 30
    environment:
      node: latest
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Expo Export
        script: npx expo export
    artifacts:
      - dist/**/*
